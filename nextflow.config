manifest {
    name = 'nf-probeset'
    mainScript = 'main.nf'
}

params.selector_kwargs = [:]
params.selector_config = null

process {
    executor = 'local'
    shell = ['/bin/bash', '-euo', 'pipefail']
}

profiles {
    standard {
        process.container = null
    }

    singularity {
        process.container = params.container

        singularity {
            enabled = true
            autoMounts = true
            cacheDir = "${projectDir}/.nextflow/singularity"
        }
    }
}